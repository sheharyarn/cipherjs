#!/usr/bin/env node

'use strict';

const utils = require('../utils');


// Main Ciphers
// ------------

const Caesar = {
  encrypt: (plaintext, key) => {
  },

  decrypt: (ciphertext, key) => {
  }
};



// Export Modules
// --------------

module.exports = {
  Substitution: Caesar
};

